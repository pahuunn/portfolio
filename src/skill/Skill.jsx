import React from 'react';
import SkillCard from './SkillCard';

const Skill = (props) => {
    return (
        <section>
            <h1>Skill</h1>
            <p>保有スキルの紹介</p>
            <SkillCard />
        </section>
    );
};
export default Skill;