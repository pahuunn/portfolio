import React from 'react';

import './Header.css'

const Header = (props) => {
    return (
        <header className="Header">
            <h1>Hiroaki Yoshida's Portfolio</h1>
            <p className="Mine"></p>
        </header>
    );
};

export default Header;